class Solution(object):
    def defangIPaddr(self, address):
        """
        :type address: str
        :rtype: str
        """
        l=list(address)
        for i in range(len(l)):
            if l[i]=='.':
                l[i]="[.]"
        return "".join(l)
