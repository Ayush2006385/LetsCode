class Solution(object):
    def commonFactors(self, a, b):
        """
        :type a: int
        :type b: int
        :rtype: int
        """
        l=[]
        for i in range(1,a+1):
            if a%i==0:
                l.append(i)
        f=[]
        for i in range(1,b+1):
            if b%i==0:
                f.append(i)
        print(l)
        print(f)
        a=list(set(l) & set(f))
        return len(a)
